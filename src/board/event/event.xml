<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="event">
    
    <typeAlias alias="event_vo" type="board.event.EventVO"/>
    
    <!-- 공통조건 -->
	<sql id="searchSql">		
		<!-- WHERE 절 검색 -->
		<dynamic prepend="where">
			1=1
			<isNotEqual property="sdisplay" compareValue="-1">
			    and display = #sdisplay#
			</isNotEqual>
			<!-- 검색어 검색 -->
			<isNotEqual property="sval" compareValue="">
				<!-- 검색 타입이 all일 때 -->
				<isEqual property="stype" compareValue="all" prepend="and">
					and ( (contents like '%$sval$%' ) or (event_name like '%$sval$%' ) ) 					
				</isEqual>
				<!-- 검색 타입이 all이 아닐때 해당 stype으로 검색 -->
				<isNotEqual property="stype" compareValue="all" prepend="and">
					and ($stype$ like '%$sval$%' )
				</isNotEqual>
			</isNotEqual>
		</dynamic>
	</sql>
	
	<!-- 해당 테이블의 총 게시글 수 -->
	<select id="count" parameterClass="event_vo" resultClass="Integer">
        select count(*) from $tablename$
        <include refid="searchSql" />	
    </select>
    
	<select id="list" parameterClass="event_vo" resultClass="event_vo">
        select *,
        (select name from admin where admin.no=event.writer) as writer_name
        from $tablename$
        <include refid="searchSql" />
		ORDER BY cre_date DESC LIMIT $startPageNo$, $pageRows$
    </select>
	
	<!-- 해당 테이블에 게시글 작성 -->
    <insert id="insert" parameterClass="event_vo">
        INSERT INTO $tablename$ (
        	writer, event_name, contents, cre_date, readno, sta_date, end_date,
        	filename_org, filename, filesize, display
        ) VALUES (
        	#writer#, #event_name#, #contents#, now(), #readno#, #sta_date#, #end_date#,
        	#filename_org#, #filename#, #filesize#, #display#
        )
        <selectKey keyProperty="no" resultClass="Integer">
        	SELECT LAST_INSERT_ID()
        </selectKey>
    </insert>
    
    <update id="update" parameterClass="event_vo">
        UPDATE $tablename$ SET
        	 writer=#writer#, event_name=#event_name#, display=#display#, sta_date=#sta_date#, end_date=#end_date#,
        <isNotEqual property="cre_date" compareValue="">
       	    cre_date = #cre_date#,
       	</isNotEqual>
        <isNotEqual property="readno" compareValue="0">
       	    readno = #readno#,
       	</isNotEqual>        	
        <isEqual property="filename_chk" compareValue="1">
        	filename='', filename_org='', filesize=0,
        </isEqual>
        <isNotEqual property="filename" compareValue="">
            <isNotNull property="filename">
        		filename=#filename#, filename_org=#filename_org#, filesize=#filesize#,
        	</isNotNull>
        </isNotEqual>
        	contents=#contents#
        WHERE no = #no#
    </update>
    
    <select id="read" parameterClass="event_vo" resultClass="event_vo">
        SELECT 
        	*
        FROM $tablename$
        where no = #no#
    </select>
    
    <select id="filenames" parameterClass="event_vo" resultClass="event_vo">
    	<![CDATA[
        SELECT 
        	file_name
        FROM $tablename$
        where no = #no#
        ]]>
    </select> 
        
    <select id="getRowNum" parameterClass="event_vo" resultClass="Integer">
        select rownum from (
        	select @rownum:=@rownum+1 as rownum, no, title from $tablename$, (select @rownum:=0) as r
        	<include refid="searchSql" />
        	ORDER BY cre_date DESC
        ) as r2
        where r2.no = #no#
    </select>
    
    <select id="getNextRowNum" parameterClass="event_vo" resultClass="event_vo">
        select ifnull(rownum,0), ifnull(no,0) as next_no, title as next_title
        from (
        	select @rownum:=@rownum+1 as rownum, no, title from $tablename$, (select @rownum:=0) as r
        	<include refid="searchSql" />
        	ORDER BY cre_date DESC
        ) as r2
        <![CDATA[
        where r2.rownum = $rownum$+1
    	]]>
    </select>
    
    <select id="getPrevRowNum" parameterClass="event_vo" resultClass="event_vo">
    	select ifnull(rownum,0), ifnull(no,0) as prev_no, title as prev_title
        from (
        	select @rownum:=@rownum+1 as rownum, no, title from $tablename$, (select @rownum:=0) as r
        	<include refid="searchSql" />
        	ORDER BY cre_date DESC
        ) as r2
        <![CDATA[
        where r2.rownum = $rownum$-1
    	]]>
    </select>
    
    <delete id="delete" parameterClass="event_vo">
        DELETE FROM $tablename$ WHERE no=#no#
    </delete>
    
    <update id="updateReadno" parameterClass="event_vo">
        UPDATE $tablename$ SET readno = readno+1 WHERE no = #no#
    </update>
    
    <select id="mainList" parameterClass="event_vo" resultClass="event_vo">
        select *
        from $tablename$
        <include refid="searchSql" />
		ORDER BY cre_date DESC LIMIT 0, $number$
    </select>
    
</sqlMap>